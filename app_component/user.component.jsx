import { StatusBar } from "expo-status-bar";
import React from "react";
import {
  StyleSheet,
  Text,
  View,
  ActivityIndicator,
  SafeAreaView,
  FlatList,
  Button,
} from "react-native";

const user= (porps)=>{

    <View>
        <Text>{this.props.userId}</Text>
    </View>

}